<h1>Listing reservations</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @reservations.each do |reservation| %>
  <tr>
    <td><%= link_to 'Show', hotel_room_reservation_path(reservation) %></td>
    <td><%= link_to 'Edit', edit_hotel_room_reservation_path(reservation) %></td>
    <td><%= link_to 'Destroy', reservation, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Reservation', new_hotel_room_reservation_path %>
